import React from 'react'
import Layout from '../components/Layout'

export type ReposPageProps = {}

const ReposPage = ({}: ReposPageProps) => {
  return <Layout>Repos</Layout>
}

export default ReposPage
